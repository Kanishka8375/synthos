# ─── Supabase (free at supabase.com) ──────────────────────────────────────────
# 1. Go to https://supabase.com and create a free project
# 2. Settings → API → copy Project URL and anon/public key
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_YOUR_KEY

# For DB migrations (optional — only needed to run scripts/migrate.mjs or /api/setup)
SUPABASE_SERVICE_ROLE_KEY=sb_secret_YOUR_SERVICE_ROLE_KEY
SUPABASE_DB_URL=postgresql://postgres:YOUR_PASSWORD@db.YOUR_PROJECT_ID.supabase.co:5432/postgres

# ─── Hugging Face (free at huggingface.co) ────────────────────────────────────
# 1. Go to https://huggingface.co/settings/tokens
# 2. Create a new token (read access is enough)
HUGGINGFACE_TOKEN=hf_YOUR_TOKEN_HERE

# ─── OpenRouter (openrouter.ai) ───────────────────────────────────────────────
# 1. Go to https://openrouter.ai and create a free account
# 2. Settings → Keys → Create key
# Used for all LLM text generation (scripts, lore, backstories, trend ideas)
OPENROUTER_API_KEY=sk-or-v1-YOUR_KEY_HERE

# ─── Pollinations.ai ──────────────────────────────────────────────────────────
# Get a free API key at https://enter.pollinations.ai
# Secret key (sk_) for server-side use only — unlocks flux model + higher limits
POLLINATIONS_API_KEY=sk_YOUR_KEY_HERE
